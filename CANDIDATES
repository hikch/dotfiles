# Deploy targets (whitelist)
# Files and directories to be symlinked to $HOME
# One per line, comments start with #

# Dotfiles (pattern: .??* means .files with 2+ chars)
.??*

# Explicit directories
bin
etc

# Claude configuration file only
.claude/CLAUDE.md