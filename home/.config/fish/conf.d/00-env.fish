# Environment Variables
# Managed explicitly instead of Universal Variables for better Git control
# Note: EDITOR and SHELL are set in config.fish
# Note: PYENV_ROOT and tool-specific PATHs are set in config.fish with tool init

# Python virtual environment
set -gx VIRTUAL_ENV_DISABLE_PROMPT 1
