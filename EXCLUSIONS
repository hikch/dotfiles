# Deploy exclusions
# Files and directories to exclude from deployment
# One per line, comments start with #

# Git and system files
.DS_Store
.git
.gitmodules
.gitignore
.travis.yml

# Internal dotfiles tools
nix

# Claude settings (user-specific)
.claude

# Partial links (handled separately)
# Note: PARTIAL_LINKS are defined in Makefile
# .local